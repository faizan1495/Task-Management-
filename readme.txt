backend:-
configur the application.yml file
    set the server port number to 8081 or you can set the port number
MySql configuration-
    create the database name with task_management
    set the username and password

then run the program 
make sure the java program is running and then run the index.html file

frontend:-
    step:-
        1-set the port number on live server if needed
        2-run the index.html file on live server
        3-clike the add-task button
        4-add the task and click create button


